# Shared secrets: never commit (the real .env lives one level up, at
# topics/voice_agents/.env; this entry guards against a stray local copy too).
.env

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# uv can regenerate this; keep or commit per the pyproject note.
# (Left un-ignored on purpose: committing uv.lock pins the environment.)

# Node / Next.js (not used in this module, kept for consistency across modules)
node_modules/
.next/
out/
