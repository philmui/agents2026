# Shared secrets — never commit (the real .env lives at topics/voice_agents/.env)
.env

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Node / Next.js (not used in this module, kept for course consistency)
node_modules/
.next/
out/
